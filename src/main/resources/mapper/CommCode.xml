<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dage.recruituser.DAO.CommCodeDAO">
    <select id="selectCommCodeList" resultType="dage.recruituser.DTO.CommCodeDTO">
        SELECT DTL_VALUE_01 AS id, DTL_NAME AS TEXT
        FROM COMM_CODE A
        LEFT JOIN COMM_CODE_DTL B
        ON A.GRP_ID = B.GRP_ID
        WHERE A.GRP_NAME = #{GRP_NAME}
        ORDER BY B.DTL_CODE
    </select>

    <select id="selectAppList" resultType="dage.recruituser.DTO.AppBoardDTO">
        SELECT APP_NO AS id, APP_TITLE AS TEXT
        FROM APP_BOARD
        WHERE APP_NO = #{APP_NO}
        ORDER BY APP_NO DESC
    </select>
</mapper>